<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.roomdatabase">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Roomdatabase">
        <activity android:name=".FridayAdapterr"></activity>
        <activity android:name=".FridayAdapter" />
        <activity android:name=".FridayViewHolder" />
        <activity android:name=".FridayModel" />
        <activity android:name=".MainActivity2" />
        <activity android:name=".Fragment3" />
        <activity android:name=".Fragment2" />
        <activity android:name=".Fragment1" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>